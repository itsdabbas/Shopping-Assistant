# apps/ — Application layer
# Each subdirectory is a self-contained demo application that uses
# the gazeshop.toolkit core.  Applications must not import from each
# other; they may only import from gazeshop.toolkit.
